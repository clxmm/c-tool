# 需求：
密码生产器，可以根据规则生产密码
## 规则
1.可以选择长度，默认8位密码
2.可以选择密码数量，默认5组
4.可以选择是否包含，数字、小写、大写（注：可以单独选择一个或者多个，可以单独设置生产的结果中分别包含几个，默认1个）
4.可以选择是否添加特殊字符，默认给用户几个，如?,!,@,等等，用户可自行修改，选择添加默认字符的时候，可以选择生产的结果中包含几个，默认1个
生产的密码支持单独的一键复制
